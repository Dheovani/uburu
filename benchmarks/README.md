# Benchmarks

Este diretório receberá benchmarks reproduzíveis de scan, busca literal/regex, tempo até o primeiro resultado, indexação e reconciliação incremental. Cada benchmark deverá registrar dataset, hardware, configuração e orçamento de memória.
