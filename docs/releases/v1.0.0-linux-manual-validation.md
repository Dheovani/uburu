# Uburu Linux manual-validation evidence

> This record contains aggregate evidence only. Private dataset paths, matching file names, and document content are intentionally omitted.

## Run metadata

- Recording: `tests/recordings/linux.mp4`
- Recording duration: approximately 1 minute and 54 seconds
- Artifact: Linux x86-64 AppImage
- Dataset profiles: multi-gigabyte real-world PDF corpus and the Uburu code repository
- Complementary automated evidence: `v1.0.0-linux-document-corpus.md` and `v1.0.0-linux-uburu-repository.md`

## Observed product behavior

- The AppImage launched on the supported Linux environment outside the build tree.
- Recursive PDF search found the known document content and populated results progressively.
- Result selection loaded the occurrence-aware preview without the Windows section-formatting defect found earlier in the validation cycle.
- Menus, command palette, result navigation, cancellation, and window interaction remained responsive.
- The formal Release CLI runs converged across direct, indexed, and hybrid strategies for both the document corpus and code repository.
- The rebuilt indexes remained fresh, and unchanged incremental passes reused all catalog entries.

## Performance observation

On the same 32-logical-core, approximately 32 GiB hardware, the formal PDF-corpus run completed materially faster on Linux than the corresponding Windows run. This is recorded as a platform baseline, not yet attributed to a specific filesystem, toolchain, extraction, or scheduling cause.

## Decision

- Status: **Passed**
- No crash, assertion, out-of-scope result, stale final result, or blocking UI failure was reported during the Linux cycle.
- The Windows and Linux representative-dataset validation cycles are complete.
